The PC side program is made in Python3 so it's mandatory install it.

To execute the program from terminal :
1 Run mapping.py

To execute the program with GUI :
1 Go to sonarQt/sonar/
2 Run main.py (from Linux is available the bash file "exec.py")
